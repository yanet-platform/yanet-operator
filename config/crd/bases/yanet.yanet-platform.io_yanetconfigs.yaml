---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: yanetconfigs.yanet.yanet-platform.io
spec:
  group: yanet.yanet-platform.io
  names:
    categories:
    - yanet
    kind: YanetConfig
    listKind: YanetConfigList
    plural: yanetconfigs
    shortNames:
    - yntcfg
    singular: yanetconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.updateWindow
      name: UpdateWindow
      type: integer
    - jsonPath: .spec.stop
      name: Stop
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: YanetConfig is the Schema for the yanetconfigs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              YanetConfigSpec is the cluster-wide knowledge base. It defines:
                - which components exist (spec.components)
                - the named registry of strategic-merge patches (spec.patches)
                - box presets that wire components and patches together
                  (spec.boxTypes).

              A Yanet CR references a boxType by name; everything else is derived
              from this YanetConfig.
            properties:
              boxTypes:
                description: |-
                  BoxTypes are box presets, each wiring components to lists of
                  patches by name. A Yanet CR references one entry by name.
                items:
                  description: BoxType is a named preset wiring components to patch
                    lists.
                  properties:
                    components:
                      description: |-
                        Components defines which fixed workload components are
                        enabled and which patches each receives.
                      properties:
                        birdAdapter:
                          description: BoxComponent is the per-component slot in a
                            boxType.
                          properties:
                            patches:
                              description: |-
                                Patches lists patch names from YanetConfig.spec.patches[].
                                Patches are applied in declared order.
                              items:
                                type: string
                              type: array
                          type: object
                        controlplane:
                          description: BoxComponent is the per-component slot in a
                            boxType.
                          properties:
                            patches:
                              description: |-
                                Patches lists patch names from YanetConfig.spec.patches[].
                                Patches are applied in declared order.
                              items:
                                type: string
                              type: array
                          type: object
                        dataplane:
                          description: |-
                            BoxDataplane is the per-box slot for the dataplane Deployment and its declared
                            native sidecars. Patches apply to the whole Deployment, including sidecars.
                          properties:
                            patches:
                              description: |-
                                Patches lists patch names from YanetConfig.spec.patches[]. Patches are
                                applied to the dataplane Deployment in declared order.
                              items:
                                type: string
                              type: array
                            sidecars:
                              additionalProperties:
                                description: |-
                                  BoxDataplaneSidecar selects a sidecar for a box type. A present slot defaults
                                  to enabled; enabled=false keeps the declaration explicit while omitting the
                                  sidecar from the rendered Pod.
                                properties:
                                  enabled:
                                    default: true
                                    type: boolean
                                  patches:
                                    description: Patches address this sidecar's logical
                                      container and volume names.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              description: |-
                                Sidecars selects native sidecars declared in
                                YanetConfig.spec.components.dataplane.sidecars.
                              type: object
                          type: object
                      type: object
                    name:
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    operators:
                      additionalProperties:
                        description: BoxOperator is the per-operator slot in a boxType.
                        properties:
                          patches:
                            items:
                              type: string
                            type: array
                        type: object
                      description: Operators is keyed by OperatorSpec.Name.
                      type: object
                  required:
                  - components
                  - name
                  type: object
                minItems: 1
                type: array
              components:
                description: |-
                  Components is the palette of available workload components plus a
                  dynamic operators[] array. The dataplane slot describes one Pod with
                  explicitly declared native sidecars.
                properties:
                  birdAdapter:
                    description: |-
                      BirdAdapter is a SEPARATE Deployment (not a sidecar to bird),
                      so the adapter can be updated without restarting bird.
                      bird ↔ birdAdapter share the bird unix socket via a hostPath.
                      Its socket mounts are configured explicitly through patches.
                    properties:
                      config:
                        description: |-
                          ConfigSource describes where the configuration of a yanet component
                          comes from. Exactly one of Inline or HostPath must be specified.

                            - Inline: configuration is embedded into the CR; the operator creates a
                              ConfigMap (named deterministically by content hash) and mounts it into
                              the Pod at MountPath (default /etc/yanet2).
                            - HostPath: a HOST directory mounted into the Pod as a hostPath volume at
                              configured MountPath (default /etc/yanet2). The component binary finds its
                              config file inside that directory by its own default name (e.g.
                              controlplane.conf). This is the default for production hosts.

                          Args, when set, are passed to the component binary verbatim. For HostPath
                          sources the whole directory is mounted, so args can reference any file in
                          that directory. Inline content is available as <mountDir>/config.

                          Validation that exactly one of Inline/HostPath is filled is enforced
                          by the webhook.
                        properties:
                          args:
                            description: |-
                              Args defines command-line arguments passed to the component verbatim.
                              Examples: ["/etc/yanet2/dataplane.yaml"] for dataplane, ["-c",
                              "/etc/yanet2/controlplane.yaml"] for controlplane and ["server", "-c",
                              "/etc/yanet2/bird-adapter.yaml"] for bird-adapter.
                            items:
                              type: string
                            type: array
                          hostPath:
                            description: |-
                              HostPath is the HOST directory to mount into the container via a
                              hostPath volume. The directory is mounted read-only at the
                              default path /etc/yanet2, unless MountPath or a patch customizes it.
                              The component binary reads its config file from
                              inside that directory using its own default file name.
                            type: string
                          inline:
                            description: |-
                              Inline is the literal configuration body. When set, the operator
                              creates a ConfigMap owned by the CR and mounts it into the Pod.
                            type: string
                          mountPath:
                            description: |-
                              MountPath is the absolute container directory for the managed config volume.
                              Defaults to /etc/yanet2 when omitted. Inline content is stored as config
                              inside this directory; Args are never rewritten to match the mount.
                            pattern: ^(/.*)?$
                            type: string
                        type: object
                      image:
                        description: |-
                          ImageRef identifies a palette image. Registry and prefix default to
                          YanetConfig.spec.images but can be overridden independently for each image.
                        properties:
                          name:
                            description: Name is the image name without registry/prefix/tag.
                            minLength: 1
                            type: string
                          prefix:
                            description: |-
                              Prefix overrides the global prefix. Nil inherits the global value;
                              an explicit empty string omits the prefix segment.
                            type: string
                          registry:
                            description: |-
                              Registry overrides the global registry. Nil inherits the global value;
                              an explicit empty string omits the registry segment.
                            type: string
                          tag:
                            description: Tag is the image tag.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - image
                    type: object
                  controlplane:
                    description: |-
                      ControlplaneSpec describes the controlplane component. Multi-NUMA nodes get
                      one Deployment and one stable Service per NUMA domain.
                    properties:
                      config:
                        description: Config is the configuration source (inline |
                          hostPath).
                        properties:
                          args:
                            description: |-
                              Args defines command-line arguments passed to the component verbatim.
                              Examples: ["/etc/yanet2/dataplane.yaml"] for dataplane, ["-c",
                              "/etc/yanet2/controlplane.yaml"] for controlplane and ["server", "-c",
                              "/etc/yanet2/bird-adapter.yaml"] for bird-adapter.
                            items:
                              type: string
                            type: array
                          hostPath:
                            description: |-
                              HostPath is the HOST directory to mount into the container via a
                              hostPath volume. The directory is mounted read-only at the
                              default path /etc/yanet2, unless MountPath or a patch customizes it.
                              The component binary reads its config file from
                              inside that directory using its own default file name.
                            type: string
                          inline:
                            description: |-
                              Inline is the literal configuration body. When set, the operator
                              creates a ConfigMap owned by the CR and mounts it into the Pod.
                            type: string
                          mountPath:
                            description: |-
                              MountPath is the absolute container directory for the managed config volume.
                              Defaults to /etc/yanet2 when omitted. Inline content is stored as config
                              inside this directory; Args are never rewritten to match the mount.
                            pattern: ^(/.*)?$
                            type: string
                        type: object
                      disabledNuma:
                        description: |-
                          DisabledNuma lists NUMA indices that must NOT get a
                          controlplane instance within the configured fan-out.
                          The usual reason is a NUMA domain without any NIC: the
                          dataplane runs no instance there, so a controlplane for it
                          would have no dataplane peer to attach to.

                          Indices are zero-based and refer to the same numbering as the
                          configured NUMA fan-out (`numa`, default 1). Out-of-range indices
                          are ignored, duplicates are collapsed. Disabling every index
                          is rejected by the webhook.

                          This is the cluster-wide default; a single installation can
                          override it via
                          Yanet.spec.components.controlplane.disabledNuma.
                        items:
                          format: int32
                          type: integer
                        type: array
                      image:
                        description: |-
                          ImageRef identifies a palette image. Registry and prefix default to
                          YanetConfig.spec.images but can be overridden independently for each image.
                        properties:
                          name:
                            description: Name is the image name without registry/prefix/tag.
                            minLength: 1
                            type: string
                          prefix:
                            description: |-
                              Prefix overrides the global prefix. Nil inherits the global value;
                              an explicit empty string omits the prefix segment.
                            type: string
                          registry:
                            description: |-
                              Registry overrides the global registry. Nil inherits the global value;
                              an explicit empty string omits the registry segment.
                            type: string
                          tag:
                            description: Tag is the image tag.
                            type: string
                        required:
                        - name
                        type: object
                      numa:
                        description: |-
                          Numa is the configured controlplane NUMA fan-out per node (1 through 4).
                          Defaults to 1 when omitted. Set it explicitly for multi-NUMA hosts.
                        format: int32
                        maximum: 4
                        minimum: 1
                        type: integer
                    required:
                    - image
                    type: object
                  dataplane:
                    description: |-
                      DataplaneSpec describes one dataplane Pod: the DPDK process, hugepages and
                      declared native sidecars that share its private network namespace.
                    properties:
                      config:
                        description: |-
                          ConfigSource describes where the configuration of a yanet component
                          comes from. Exactly one of Inline or HostPath must be specified.

                            - Inline: configuration is embedded into the CR; the operator creates a
                              ConfigMap (named deterministically by content hash) and mounts it into
                              the Pod at MountPath (default /etc/yanet2).
                            - HostPath: a HOST directory mounted into the Pod as a hostPath volume at
                              configured MountPath (default /etc/yanet2). The component binary finds its
                              config file inside that directory by its own default name (e.g.
                              controlplane.conf). This is the default for production hosts.

                          Args, when set, are passed to the component binary verbatim. For HostPath
                          sources the whole directory is mounted, so args can reference any file in
                          that directory. Inline content is available as <mountDir>/config.

                          Validation that exactly one of Inline/HostPath is filled is enforced
                          by the webhook.
                        properties:
                          args:
                            description: |-
                              Args defines command-line arguments passed to the component verbatim.
                              Examples: ["/etc/yanet2/dataplane.yaml"] for dataplane, ["-c",
                              "/etc/yanet2/controlplane.yaml"] for controlplane and ["server", "-c",
                              "/etc/yanet2/bird-adapter.yaml"] for bird-adapter.
                            items:
                              type: string
                            type: array
                          hostPath:
                            description: |-
                              HostPath is the HOST directory to mount into the container via a
                              hostPath volume. The directory is mounted read-only at the
                              default path /etc/yanet2, unless MountPath or a patch customizes it.
                              The component binary reads its config file from
                              inside that directory using its own default file name.
                            type: string
                          inline:
                            description: |-
                              Inline is the literal configuration body. When set, the operator
                              creates a ConfigMap owned by the CR and mounts it into the Pod.
                            type: string
                          mountPath:
                            description: |-
                              MountPath is the absolute container directory for the managed config volume.
                              Defaults to /etc/yanet2 when omitted. Inline content is stored as config
                              inside this directory; Args are never rewritten to match the mount.
                            pattern: ^(/.*)?$
                            type: string
                        type: object
                      hugepages:
                        description: Hugepages requested by the Pod.
                        properties:
                          count:
                            description: Count is the number of hugepages requested.
                            format: int32
                            minimum: 1
                            type: integer
                          size:
                            description: Size of a single hugepage (e.g. "1Gi", "2Mi").
                            type: string
                        required:
                        - count
                        - size
                        type: object
                      image:
                        description: |-
                          ImageRef identifies a palette image. Registry and prefix default to
                          YanetConfig.spec.images but can be overridden independently for each image.
                        properties:
                          name:
                            description: Name is the image name without registry/prefix/tag.
                            minLength: 1
                            type: string
                          prefix:
                            description: |-
                              Prefix overrides the global prefix. Nil inherits the global value;
                              an explicit empty string omits the prefix segment.
                            type: string
                          registry:
                            description: |-
                              Registry overrides the global registry. Nil inherits the global value;
                              an explicit empty string omits the registry segment.
                            type: string
                          tag:
                            description: Tag is the image tag.
                            type: string
                        required:
                        - name
                        type: object
                      networks:
                        description: |-
                          Networks declares ordered device-backed Multus attachments. Each entry
                          reserves one device from ResourceName in the primary dataplane container.
                          Yanet can replace this list per installation. NADs are managed externally.
                        items:
                          description: NetworkAttachment couples one Multus attachment
                            with one extended resource.
                          properties:
                            interface:
                              description: Interface is the interface name inside
                                the dataplane Pod.
                              maxLength: 15
                              minLength: 1
                              type: string
                            name:
                              description: Name references an existing NetworkAttachmentDefinition.
                              maxLength: 253
                              minLength: 1
                              type: string
                            namespace:
                              description: Namespace defaults to the Yanet installation
                                namespace.
                              maxLength: 63
                              type: string
                            resourceName:
                              description: ResourceName must match the NAD's k8s.v1.cni.cncf.io/resourceName
                                annotation.
                              minLength: 1
                              type: string
                          required:
                          - interface
                          - name
                          - resourceName
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      sidecars:
                        description: |-
                          Sidecars is the ordered palette of single-container native sidecars.
                          Each declaration reserves a gRPC/HTTP pair at 8080+2*i / 8081+2*i,
                          including unselected and disabled entries. The box type selects names;
                          it never changes their declared order or port indices.
                        items:
                          description: |-
                            SidecarSpec describes exactly one native sidecar container in the dataplane.
                            Names are unique across sidecars and standalone operators.
                          properties:
                            config:
                              description: |-
                                ConfigSource describes where the configuration of a yanet component
                                comes from. Exactly one of Inline or HostPath must be specified.

                                  - Inline: configuration is embedded into the CR; the operator creates a
                                    ConfigMap (named deterministically by content hash) and mounts it into
                                    the Pod at MountPath (default /etc/yanet2).
                                  - HostPath: a HOST directory mounted into the Pod as a hostPath volume at
                                    configured MountPath (default /etc/yanet2). The component binary finds its
                                    config file inside that directory by its own default name (e.g.
                                    controlplane.conf). This is the default for production hosts.

                                Args, when set, are passed to the component binary verbatim. For HostPath
                                sources the whole directory is mounted, so args can reference any file in
                                that directory. Inline content is available as <mountDir>/config.

                                Validation that exactly one of Inline/HostPath is filled is enforced
                                by the webhook.
                              properties:
                                args:
                                  description: |-
                                    Args defines command-line arguments passed to the component verbatim.
                                    Examples: ["/etc/yanet2/dataplane.yaml"] for dataplane, ["-c",
                                    "/etc/yanet2/controlplane.yaml"] for controlplane and ["server", "-c",
                                    "/etc/yanet2/bird-adapter.yaml"] for bird-adapter.
                                  items:
                                    type: string
                                  type: array
                                hostPath:
                                  description: |-
                                    HostPath is the HOST directory to mount into the container via a
                                    hostPath volume. The directory is mounted read-only at the
                                    default path /etc/yanet2, unless MountPath or a patch customizes it.
                                    The component binary reads its config file from
                                    inside that directory using its own default file name.
                                  type: string
                                inline:
                                  description: |-
                                    Inline is the literal configuration body. When set, the operator
                                    creates a ConfigMap owned by the CR and mounts it into the Pod.
                                  type: string
                                mountPath:
                                  description: |-
                                    MountPath is the absolute container directory for the managed config volume.
                                    Defaults to /etc/yanet2 when omitted. Inline content is stored as config
                                    inside this directory; Args are never rewritten to match the mount.
                                  pattern: ^(/.*)?$
                                  type: string
                              type: object
                            image:
                              description: |-
                                ImageRef identifies a palette image. Registry and prefix default to
                                YanetConfig.spec.images but can be overridden independently for each image.
                              properties:
                                name:
                                  description: Name is the image name without registry/prefix/tag.
                                  minLength: 1
                                  type: string
                                prefix:
                                  description: |-
                                    Prefix overrides the global prefix. Nil inherits the global value;
                                    an explicit empty string omits the prefix segment.
                                  type: string
                                registry:
                                  description: |-
                                    Registry overrides the global registry. Nil inherits the global value;
                                    an explicit empty string omits the registry segment.
                                  type: string
                                tag:
                                  description: Tag is the image tag.
                                  type: string
                              required:
                              - name
                              type: object
                            listeners:
                              description: |-
                                Listeners declares Service exposure, defaulting to grpc. An explicit
                                empty list creates no Service but retains the slot and host-config env.
                              items:
                                description: OperatorListener is a supported application
                                  listener.
                                enum:
                                - grpc
                                - http
                                type: string
                              maxItems: 2
                              type: array
                              x-kubernetes-list-type: set
                            name:
                              maxLength: 63
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                              type: string
                          required:
                          - image
                          - name
                          type: object
                        maxItems: 28728
                        type: array
                        x-kubernetes-list-type: atomic
                    required:
                    - image
                    type: object
                  operators:
                    description: |-
                      Operators are dynamic, keyed by Name. Each is rendered as one
                      Deployment and one Service.
                    items:
                      description: OperatorSpec describes one independently deployed
                        operator container group.
                      properties:
                        containers:
                          description: |-
                            Containers lists the containers of the Pod. At least one is
                            required.
                          items:
                            description: OperatorContainer describes one container
                              of an operator Pod.
                            properties:
                              config:
                                description: Config is the configuration source for
                                  this container.
                                properties:
                                  args:
                                    description: |-
                                      Args defines command-line arguments passed to the component verbatim.
                                      Examples: ["/etc/yanet2/dataplane.yaml"] for dataplane, ["-c",
                                      "/etc/yanet2/controlplane.yaml"] for controlplane and ["server", "-c",
                                      "/etc/yanet2/bird-adapter.yaml"] for bird-adapter.
                                    items:
                                      type: string
                                    type: array
                                  hostPath:
                                    description: |-
                                      HostPath is the HOST directory to mount into the container via a
                                      hostPath volume. The directory is mounted read-only at the
                                      default path /etc/yanet2, unless MountPath or a patch customizes it.
                                      The component binary reads its config file from
                                      inside that directory using its own default file name.
                                    type: string
                                  inline:
                                    description: |-
                                      Inline is the literal configuration body. When set, the operator
                                      creates a ConfigMap owned by the CR and mounts it into the Pod.
                                    type: string
                                  mountPath:
                                    description: |-
                                      MountPath is the absolute container directory for the managed config volume.
                                      Defaults to /etc/yanet2 when omitted. Inline content is stored as config
                                      inside this directory; Args are never rewritten to match the mount.
                                    pattern: ^(/.*)?$
                                    type: string
                                type: object
                              image:
                                description: |-
                                  ImageRef identifies a palette image. Registry and prefix default to
                                  YanetConfig.spec.images but can be overridden independently for each image.
                                properties:
                                  name:
                                    description: Name is the image name without registry/prefix/tag.
                                    minLength: 1
                                    type: string
                                  prefix:
                                    description: |-
                                      Prefix overrides the global prefix. Nil inherits the global value;
                                      an explicit empty string omits the prefix segment.
                                    type: string
                                  registry:
                                    description: |-
                                      Registry overrides the global registry. Nil inherits the global value;
                                      an explicit empty string omits the registry segment.
                                    type: string
                                  tag:
                                    description: Tag is the image tag.
                                    type: string
                                required:
                                - name
                                type: object
                              name:
                                description: |-
                                  Name of the container. Must be unique within the operator and
                                  is the key used by Yanet.spec.components.operators[].containers
                                  for per-container image overrides.
                                maxLength: 63
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                                type: string
                            required:
                            - image
                            - name
                            type: object
                          maxItems: 8
                          minItems: 1
                          type: array
                        listeners:
                          description: |-
                            Listeners are owned by the first container. Omitted defaults to grpc.
                            An empty list means
                            no listener and no Service. Explicit lists are rendered in grpc/http order.
                          items:
                            description: OperatorListener is a supported application
                              listener.
                            enum:
                            - grpc
                            - http
                            type: string
                          maxItems: 2
                          type: array
                          x-kubernetes-list-type: set
                        name:
                          description: |-
                            Name is unique within the Operators array. It is used as the component
                            label and default container name. Built-in component names are reserved.
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                      required:
                      - containers
                      - name
                      type: object
                    type: array
                required:
                - controlplane
                - dataplane
                type: object
              images:
                description: |-
                  Images defines global image settings shared by all generated
                  Deployments.
                properties:
                  prefix:
                    description: |-
                      Prefix is the default path segment between registry and image
                      name: {registry}/{prefix}/{image}:{tag}. Palette images may override it.
                    type: string
                  pullPolicy:
                    description: PullPolicy applies to every container the operator
                      generates.
                    enum:
                    - Always
                    - Never
                    - IfNotPresent
                    type: string
                  pullSecrets:
                    description: |-
                      PullSecrets are propagated to every Pod created by the
                      operator.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  registry:
                    description: Registry is the default registry for palette images
                      without an override.
                    type: string
                type: object
              patches:
                description: |-
                  Patches is the named registry of strategic-merge Deployment
                  fragments. Each patch is a slice of an appsv1.Deployment.
                items:
                  description: |-
                    NamedPatch is a strategic-merge patch fragment of an appsv1.Deployment
                    stored in the cluster-wide patch registry.
                  properties:
                    name:
                      description: Name uniquely identifies the patch within the registry.
                      minLength: 1
                      type: string
                    patch:
                      description: Patch carries the strategic-merge fragment as raw
                        JSON/YAML.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - name
                  - patch
                  type: object
                type: array
              stop:
                default: false
                description: |-
                  Stop is a global kill switch. When true, the reconcile loop
                  does nothing across the whole cluster.
                type: boolean
              updateWindow:
                default: 0
                description: |-
                  UpdateWindow is a global per-cluster throttling between any
                  two node restarts. Expressed in seconds. After a restart on
                  any node, the reconciler delays the next restart (anywhere)
                  by this many seconds.
                maximum: 9223372036
                minimum: 0
                type: integer
            required:
            - boxTypes
            - components
            type: object
          status:
            description: YanetConfigStatus defines the observed state of YanetConfig.
            properties:
              conditions:
                description: Conditions hold latest observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: metadata.name must be config
          rule: self.metadata.name == 'config'
    served: true
    storage: true
    subresources:
      status: {}
