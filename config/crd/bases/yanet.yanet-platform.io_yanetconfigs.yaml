---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: yanetconfigs.yanet.yanet-platform.io
spec:
  group: yanet.yanet-platform.io
  names:
    kind: YanetConfig
    listKind: YanetConfigList
    plural: yanetconfigs
    singular: yanetconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: YanetConfig is the Schema for the yanetconfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: YanetConfigSpec defines the desired state of YanetConfig
            properties:
              autodiscovery:
                description: (Optional) AutoDiscovery configure new worker node initializer.
                properties:
                  archuri:
                    description: '(Optional) Uri for arc resolver. This uri get arch
                      by node name. Example: `curl localhost:80/arch/test.yanet-platform.io`
                      -> corei7'
                    type: string
                  configsuri:
                    description: '(Optional) Uri for configs version resolver. This
                      uri get configs version by yanet version. e.g. revision of git
                      repo Example: `curl localhost:80/configs/51.2` -> 13162235'
                    type: string
                  enable:
                    default: false
                    description: '(Optional) Enable or disable discovery. Default:
                      false'
                    type: boolean
                  images:
                    description: Images name for deployments.
                    properties:
                      announcer:
                        default: announcer
                        description: '(Optional) Announcer image name. Default: announcer'
                        type: string
                      bird:
                        default: bird
                        description: '(Optional) Bird image name. Default: bird'
                        type: string
                      controlplane:
                        default: controlplane
                        description: '(Optional) Controlplane image name. Default:
                          controlplane'
                        type: string
                      dataplane:
                        default: dataplane
                        description: '(Optional) Dataplane image name. Default: dataplane'
                        type: string
                    type: object
                  namespace:
                    default: default
                    description: '(Optional) Namespace for new autogenerated Yanet
                      object. Default: default'
                    type: string
                  registry:
                    default: dockerhub.io
                    description: '(Optional) registry for new autogenerated Yanet
                      object. Default: dockerhub.io'
                    type: string
                  typeuri:
                    description: '(Optional) Uri for node type resolver. This uri
                      get node type by node name. Example: `curl localhost:80/type/test.yanet-platform.io`
                      -> release Node will be ignored if the return values is "none".'
                    type: string
                  versionuri:
                    description: '(Optional) Uri for yanet version resolver. This
                      uri get yanet version(aka docker tag) by node name. Example:
                      `curl localhost:80/version/yanet/test.yanet-platform.io` ->
                      51.2'
                    type: string
                type: object
              stop:
                default: false
                description: '(Optional) Stop means global stop. Do nothing in main
                  reconcile loop. When turned off, the operator may skip some events.
                  When turned on, we recommend restarting. Default: false'
                type: boolean
            type: object
          status:
            description: YanetConfigStatus defines the observed state of YanetConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
